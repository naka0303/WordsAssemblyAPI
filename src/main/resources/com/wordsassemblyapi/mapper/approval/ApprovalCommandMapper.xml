<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wordsassemblyapi.mapper.approval.ApprovalCommandMapper">
  <insert id="insertApproval">
    INSERT INTO approvals (
      novel_id
      , from_author_id
      , to_author_id
    ) VALUES (
      #{novelId}
      , #{dto.fromAuthorId}
      , #{dto.toAuthorId}
    )
  </insert>

  <delete id="deleteApprovalByAuthor">
    DELETE FROM approvals
    WHERE
      novel_id = #{novelId}
    AND
      from_author_id = #{authorId}
  </delete>

</mapper>